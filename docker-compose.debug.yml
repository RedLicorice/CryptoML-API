version: '3.7'

services:
  celery-worker1:
    image: redlicorice/cryptoml-celery-worker
    build:
      context: .
      dockerfile: Worker.dockerfile
    hostname: mlservice
    volumes:
      - './.env:/.env'
      - './config-docker.yml:/config.yml'
      - './cryptoml:/cryptoml'
      - './cryptoml_api:/cryptoml_api'
      - './cryptoml_common:/cryptoml_common'
    networks:
      - cryptoml-network
      - mongodb-network
      - minio-network
      - redis-network
      - influxdb-network

networks:
  cryptoml-network:
  minio-network:
    external: true
  mongodb-network:
    external: true
  redis-network:
    external: true
  influxdb-network:
    external: true